# homebrew-abs-cli

Homebrew tap for [abs-cli](https://github.com/thomaslazar/abs-cli) — a command-line interface for Audiobookshelf.

## Install

```bash
brew tap thomaslazar/abs-cli
brew install abs-cli
```

## Update

```bash
brew upgrade abs-cli
```
